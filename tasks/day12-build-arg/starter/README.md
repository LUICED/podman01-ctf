# Starter for Day 12

Build and run:

```bash
podman build -f Containerfile -t day12-solution .
podman run --rm day12-solution
```

If the output is not a flag, read the Containerfile and use the required `--build-arg`.
